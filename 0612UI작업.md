# 0612 UI 작업 명세서

> 이 문서는 **UI 개선 작업을 별도 모델/세션에서 수행하기 위한 단독 명세서**입니다.
> 작성 시점에 백엔드(`core/state_machine.py`)와 알림음 에셋은 **이미 변경 완료**되어 있습니다.
> UI 작업자는 이 문서만 읽고 `ui/ui_display.py`를 수정하면 됩니다.

---

## 0. 작업 범위 & 원칙

- **대상 파일**: 주로 [ui/ui_display.py](ui/ui_display.py) (단일 통합 pygame 대시보드). 필요 시 [main.py](main.py).
- **건드리지 말 것**: `core/` 감지 스택(Phase 1/2), `*_backup.py`.
- **진실의 원천은 상태머신**입니다. UI는 `BurnerStateMachine`의 상태·property를 **읽어서 그리기만** 합니다. 상태 전환 로직을 UI에서 새로 만들지 마세요.
- 브랜드 테마(옐로우 `(255,192,0)` / 블랙 / 화이트)는 유지.

---

## 1. 백엔드는 이미 이렇게 바뀌어 있다 (UI가 의존할 API)

UI 작업 전에 아래가 **이미 적용된 상태**임을 전제로 합니다. (`core/state_machine.py`)

### 1-1. `manual_start()` — '바로 재벌' 지원
- `DONE_FIRST`(초벌완료/냉각) 상태에서 `manual_start()`를 호출하면, **10분 냉각 대기를 건너뛰고 즉시 재벌 카운트다운(`POT_STEAMING_SECOND`)을 시작**합니다.
- 기존엔 `WAIT_SECOND`(대기)로만 점프했지만, 지금은 곧장 재벌이 시작됩니다.
- → UI는 DONE_FIRST 카드의 시작 버튼이 `manual_start()`를 호출하게만 두면 동작 완성입니다 (이미 호출 구조 존재, [ui_display.py:385-388](ui/ui_display.py#L385-L388)). **라벨만 바꾸면 됩니다** (작업 D).

### 1-2. 신규 조회 property (유동 정렬·강조용)
| property | 반환 | 용도 |
|----------|------|------|
| `bsm.is_counting` | `bool` | 초벌·재벌 카운트다운 진행 중인가 (정렬 1순위) |
| `bsm.remaining_seconds` | `float` | 카운트다운 남은 초 (정렬 키, 적을수록 위로) |
| `bsm.seconds_since_done` | `float` | 카운트다운 종료 후 경과 초. **완료 안 했으면 `-1.0`** (완료 직후 N초 상단 강조 판정) |

### 1-3. 알림음 에셋 (이미 생성됨) — 주방 소음 대응(음량 0.9, 2·3배음 혼합)
- `assets/sounds/warn_30s.wav` — **완료 30초 전 예고음** (2회 비프 + 상승음, ~0.66초) — 작업 E
- `assets/sounds/complete.wav` — **완료음** (4음 상승 팡파레, ~1.03초) — 작업 E
- `assets/sounds/cam_lost.wav` — **카메라 연결 끊김 경고음** (낮은 음 3회 반복, ~1.1초) — 작업 H
- warn_30s/complete는 초벌·재벌 구분 없이 공통. 음색/길이 변경은 루트의 [make_sounds.py](make_sounds.py)의 주파수·지속시간만 고쳐 `uv run python make_sounds.py` 재실행.

---

## 2. UI 작업 항목

### 작업 A — dev 모드에서 화구 번호 크게

**문제**: 화구가 여러 개라 영상 위 번호(`#N`)와 카드 번호가 작아 "몇 번 화구인지" 식별이 어려움.

**현재 코드**:
- dev 모드 토글은 이미 있음: `D`키 → `self.dev_mode` ([ui_display.py:283-284](ui/ui_display.py#L283-L284)).
- 카드 번호: 좌상단 작은 원 + `small` 폰트 ([L817-819](ui/ui_display.py#L817-L819)).
- 영상 오버레이 번호: `#{bid}`, `cv2.putText` 0.5 스케일 ([L584-588](ui/ui_display.py#L584-L588)).

**원하는 동작**:
- **dev 모드일 때** 영상 오버레이의 `#N` 번호를 크게(예: `cv2.putText` fontScale 0.5 → 1.2~1.5, thickness 1 → 2~3, 배경 박스도 비례 확대). 카드 번호 원/폰트도 dev 모드에서 키우면 좋음.
- 운영(비-dev) 모드에서는 현재 크기 유지(화면이 번호로 가려지지 않게).

**구현 힌트**: `self.dev_mode`로 분기해 `font_scale`, `thickness`, 배지 크기를 다르게. 폰트 dict에 큰 사이즈 항목 추가 가능([L899-901](ui/ui_display.py#L899-L901)).

---

### 작업 B — '재벌대기' 표기 중복 제거

**문제**: 한 카드에 "재벌대기"가 **두 번** 보임.

**원인 (확정)**: `WAIT_SECOND` 상태에서 `phase_label`과 `status_label`이 **둘 다 "재벌대기"**를 반환함 (`core/state_machine.py`의 두 property). UI는 이 둘을 카드의 서로 다른 위치에 그림:
- 상단 phase: [ui_display.py:832-833](ui/ui_display.py#L832-L833) (`bsm.phase_label`)
- 중앙 status: [ui_display.py:839-840](ui/ui_display.py#L839-L840) (`bsm.status_label`)

**원하는 동작**: 카드에 "재벌대기"는 **한 번만**.

**해결 방향 (택1)**:
1. **(권장, UI만 수정)** 카드 렌더에서 `phase_label`과 `status_label`이 동일 문자열이면 status 줄을 생략하거나, 역할을 분리 — 상단=단계명(phase), 중앙=시간/수치만. `WAIT_SECOND`처럼 표시할 시간이 없는 상태는 status 줄을 비우거나 안내문("딸랑이 감지 대기")으로.
2. (백엔드까지) `status_label`의 `WAIT_SECOND` 반환값을 phase와 겹치지 않게 변경. **단 백엔드 수정은 이 UI 작업 범위 밖**이므로, 가급적 방향 1로 UI에서 해결.

---

### 작업 C — RMS 수치는 개발자 모드에서만

**문제**: RMS 값이 운영 화면 카드에 항상 노출됨. 개발자만 보면 됨.

**현재 코드**:
- 카드 RMS hint: `f"RMS {bsm.current_angle:.2f}"`를 **dev 무관 항상** 표시 ([ui_display.py:842-845](ui/ui_display.py#L842-L845)).
- 영상 오버레이 RMS는 이미 `if self.dev_mode` 가드됨 ([L606-608](ui/ui_display.py#L606-L608)).

**원하는 동작**: 카드의 RMS hint도 `if self.dev_mode:`로 감쌈. 운영 모드에서는 그 자리에 진동 게이지([L847-855](ui/ui_display.py#L847-L855))만 남기거나 빈 줄. dev 모드에서만 RMS 텍스트 노출.

---

### 작업 D — '바로 재벌' 버튼

**문제**: 초벌 완료 후 바로 이어서 재벌하는 경우, 10분 냉각 대기가 불필요. 사람이 버튼으로 즉시 재벌을 시작하고 싶음.

**현재 코드**:
- DONE_FIRST 카드의 시작 버튼 라벨이 `"건너뜀"` ([ui_display.py:861](ui/ui_display.py#L861)).
- 버튼 클릭 → `manual_start()` 호출 ([L385-388](ui/ui_display.py#L385-L388)).
- **백엔드가 이미 바뀌어** `manual_start()`가 DONE_FIRST에서 즉시 재벌을 시작함(§1-1).

**원하는 동작**:
- DONE_FIRST 카드 시작 버튼 라벨을 `"건너뜀"` → **`"바로 재벌"`** 로 변경 ([L861](ui/ui_display.py#L861) `start_label`).
- 동작은 이미 연결돼 있으므로 **라벨만 바꾸면 즉시 재벌 시작**됨. 눈에 띄게(색 강조) 처리 권장.
- 확인: DONE_FIRST에서 이 버튼을 누르면 카드가 곧장 진초록(`POT_STEAMING_SECOND`, 재벌 카운트다운)으로 바뀌는지.

---

### 작업 E — 알림음 재생 (① 완료 30초 전 예고  ② 완료)

**문제**: 완료가 임박했는지, 완료됐는지 소리가 없어 놓치기 쉬움.

**원하는 동작**: 초벌·재벌 **구분 없이** 두 시점에 각각 **1회**씩 재생.
1. **완료 30초 전** — 카운트다운(초벌 또는 재벌) 남은 시간이 30초 이하로 막 내려간 순간 → `warn_30s.wav`
2. **완료 순간** — 카운트다운이 끝나 `DONE_FIRST` 또는 `DONE_SECOND`로 막 진입한 순간 → `complete.wav`

**구현 힌트**:
- `UIDisplay.init()`에서 mixer 초기화: `pygame.mixer.init()` 후 두 `pygame.mixer.Sound` 로드 (파일 없으면 graceful skip).
- 화구별 **직전 프레임 값**을 기억: `self._prev_remaining: dict[int, float] = {}`, `self._prev_states: dict[int, BurnerState] = {}`.
- 매 `render()` 카드 루프에서 각 `bsm`에 대해:
  ```python
  cur_rem  = bsm.remaining_seconds           # STEAMING 아니면 0.0 반환
  prev_rem = self._prev_remaining.get(bid, cur_rem)
  # ① 30초 전 예고 — 카운트다운 중, 30초 선을 막 통과한 프레임만
  if bsm.is_counting and prev_rem > 30 >= cur_rem > 0:
      warn_snd.play()
  # ② 완료 — STEAMING → DONE_* 진입 프레임만
  prev_st = self._prev_states.get(bid, bsm.state)
  if prev_st in (BurnerState.POT_STEAMING_FIRST, BurnerState.POT_STEAMING_SECOND) \
     and bsm.state in (BurnerState.DONE_FIRST, BurnerState.DONE_SECOND):
      complete_snd.play()
  self._prev_remaining[bid] = cur_rem
  self._prev_states[bid]    = bsm.state
  ```
- **각 1회만**: 위 "직전→현재 전이" 비교로 자동 1회 보장. 매 프레임 울리지 않게 반드시 경계 통과 프레임만.
- **예고 판정에 `is_counting`(또는 `cur_rem > 0`) 가드 필수** — `remaining_seconds`는 STEAMING이 아니면 `0.0`을 반환하므로, 가드 없으면 비카운트 상태에서 오발될 수 있음.
- '바로 재벌'로 즉시 시작한 재벌에도 같은 규칙이 그대로 적용됨(재벌 30초 전 예고 + 완료).
- 음소거 토글(예: 툴바에 `소리(N): ON/OFF`) 추가하면 운영 편의 ↑ (선택).
- 주의: pygame.mixer가 일부 환경에서 init 실패할 수 있으니 `try/except`로 감싸고 실패 시 조용히 무음 동작.

---

### 작업 F — 유동 정렬 (완료 임박순으로 좌상단)

**문제**: 카드가 항상 번호순 고정이라([ui_display.py:776](ui/ui_display.py#L776) `sorted(..., key=burner_id)`), 무언가 완료돼도 "몇 번인지" 눈으로 찾아야 함.

**원하는 동작**:
1. **카운트다운 진행 중**(`is_counting=True`) 화구를 **남은 시간(`remaining_seconds`) 적은 순**으로 좌상단부터 배치.
2. 카운트다운이 끝난 화구도 **완료 직후 약 5~10초**(`0 <= seconds_since_done <= HOLD`)는 상단 강조 그룹에 유지 → 사용자가 "방금 뭐가 됐다"를 놓치지 않게.
3. 그 외(대기·완료 후 시간 경과·빈 화구)는 **번호순**으로 하단.

**정렬 키 예시** (카드 그리드 루프 [L776-794](ui/ui_display.py#L776-L794) 교체):
```python
HOLD_S = 8.0  # 완료 후 상단 유지 시간(초). 5~10 사이로 조정.

def sort_key(b):
    if b.is_counting:
        # 1순위: 카운트다운 중 → 남은 시간 적은 순
        return (0, b.remaining_seconds, b.burner_id)
    sd = b.seconds_since_done
    if 0 <= sd <= HOLD_S:
        # 2순위: 방금 완료 → 강조 유지 (최근 완료가 위로)
        return (1, sd, b.burner_id)
    # 3순위: 나머지 → 번호순
    return (2, b.burner_id, 0)

burners = sorted(self._registry.all(), key=sort_key)
```

**주의점**:
- 카드 위치가 매 프레임 바뀌므로 클릭 타겟(`self._card_rects`, `_start_rects`, `_reset_rects`)도 **정렬 후 좌표로 재계산**되어야 함 — 현재 구조가 매 렌더 `clear()` 후 다시 채우므로([L785-794](ui/ui_display.py#L785-L794)) 그대로 두면 클릭은 정상 동작.
- 순서가 자주 바뀌면 누르려는 순간 카드가 움직여 오클릭 위험 → **번호 배지로 식별성 유지**하고, 가능하면 위치 전환에 짧은 보간(애니메이션)이나 정렬 변경 debounce를 두면 UX가 부드러움 (선택, 없어도 무방).
- `HOLD_S` 종료 후엔 자연스럽게 번호순 그룹으로 내려감(요청사항 충족).

---

### 작업 G — 패널 가시성 조정 (영상 vs 화구 카드 비중을 사용자가 제어)

**사용자가 원하는 본질**: 업장에서 카메라 영상은 "화구가 제대로 잡혔나" 확인용일 뿐 **상시 주시 대상이 아니다**. 정작 중요한 건 **각 화구의 번호와 상태**(몇 번이 몇 분 남았는지, 완료됐는지). 그런데 현재 UI는 영상이 화면 대부분을 차지하고 카드는 우측 400px 고정이라 정보가 작고 영상이 과하게 크다. → **카드(정보)를 크게 보고, 영상은 줄이거나 숨길 수 있어야 한다.** "분할선 드래그"는 목적이 아니라 그 수단 중 하나.

**필요한 것 (핵심 3가지)**:
- **G-1. 분할선 드래그** — 영상↔카드 경계를 끌어 비율을 연속 조절.
- **G-2. 영상 숨김/최소화 토글** — 키(예: `V`)·버튼으로 한 번에 "카드만 보기"로. 업장 상시 운영용. 영상을 0으로 줄이면 카드가 전 화면을 채운다.
- **G-3. 카드 크기 패널폭 연동** — 패널이 넓어지면 카드 열수·높이·번호/상태 폰트가 **실제로 커져야** 한다. (폭만 넓고 카드가 그대로면 의미 없음 — 작업 A와 직결)

**불필요한 것 (하지 말 것)**:
- 픽셀 단위 미세 프리셋·여러 단계 레이아웃 모드는 과함. **자유 드래그 + 숨김 토글이면 충분.**
- 분할선 위치 영구 저장은 "선택"(있으면 편하나 필수 아님).

**현재 코드**: 패널 폭 상수 `_RIGHT_PANEL_W = 400` 고정 ([ui_display.py:36](ui/ui_display.py#L36)), `main_w = sw - _RIGHT_PANEL_W` ([L450](ui/ui_display.py#L450)). 카드 `cols=4`/`card_h=90` 고정 ([L780-783](ui/ui_display.py#L780-L783)).

**구현 힌트**:
- `_RIGHT_PANEL_W` → 인스턴스 변수 `self._right_panel_w`(기본 400). 모든 참조 교체.
- **G-1**: 분할선 x = `sw - self._right_panel_w`, **±6px**를 드래그 핸들 hitbox로. `_on_mouse_down`에서 핸들이면 `self._dragging_divider=True` 후 **즉시 return**(카드/캘리브보다 우선). `_on_mouse_move`에서 `self._right_panel_w = clamp(sw - mx, MIN, MAX)`. `_on_mouse_up`에서 해제. 분할선에 **세로 그립 핸들(점 3개 등)** 표시로 발견성 ↑.
- **G-2**: `self._video_hidden` 토글(`V`키 / 툴바 버튼). True면 `main_w=0`으로 두어 **영상 영역 미렌더 + 감지 오버레이 연산도 스킵**(성능 이득), 카드가 전체 폭 사용. 다시 누르면 직전 비율 복귀.
- **G-3**: 카드 `cols`·`card_h`·카드 내 폰트를 `self._right_panel_w` 기준으로 산정 → 폭이 넓을수록 카드를 키워 번호·상태가 또렷하게. (작업 A의 번호 확대와 폰트 스케일 로직 공유 가능)
- 클램프 권장 `MIN=300`(카드 최소 폭), `MAX=sw`(전체 = 영상 숨김과 동일). `VIDEORESIZE`에서 재클램프.
- 카드 `cols`/`card_w`는 폭 기반([L780-782](ui/ui_display.py#L780-L782))이라 폭 변경 시 재배치는 자동.
- (선택) `self._right_panel_w`·`_video_hidden`을 `config["ui"]`에 저장해 다음 실행 유지.

---

### 작업 H — 카메라 연결 끊김 경고음 (`cam_lost.wav`)

**배경**: 카메라 프레임이 끊기면(USB/케이블 문제 등) 그 화구는 감지가 멈추므로 운영자가 즉시 알아야 함.

**현재 코드**: 프레임이 없으면 셀에 `● 프레임 없음`(빨강) + `⛔ 신호 없음` **시각 표시만** 있음 ([ui_display.py:530-548](ui/ui_display.py#L530-L548)). 소리 없음.

**원하는 동작**: 카메라가 **연결됨 → 프레임 없음으로 전환되는 순간 1회** `cam_lost.wav` 재생. (끊김이 지속되는 동안 매 프레임 반복 금지)

**구현 힌트**:
- `self._prev_frame_ok: dict[int, bool] = {}` (source_id → 직전 프레임 정상 여부).
- 카메라 그리드 루프([_draw_camera_grid](ui/ui_display.py#L489)) 또는 셀 렌더에서 `frame is None` 판정.
  - `prev_ok == True and 현재 frame is None` → `cam_lost_snd.play()` (끊김 전환).
  - 복구(None→정상)는 무음(추후 별도음 원하면 확장).
  - `self._prev_frame_ok[src_id] = (frame is not None)`로 갱신.
- 작업 E의 mixer 초기화·음소거 토글을 **공유**(같은 `pygame.mixer`, 같은 ON/OFF 플래그).
- (선택) 끊김이 길어질 때 N초(예: 30초)마다 재알림하고 싶으면 마지막 재생 시각을 기억해 주기 재생. 기본은 전환 1회.

---

## 3. 참조 — 색상·폰트·레이아웃 상수

- 브랜드/테마 색상: [ui_display.py:18-33](ui/ui_display.py#L18-L33)
- 폰트 dict (`title/id/label/timer/btn/small`): [L890-901](ui/ui_display.py#L890-L901)
- 우측 패널 폭 `_RIGHT_PANEL_W=400`, 카드 그리드 `cols=4`, `card_h=90`: [L36](ui/ui_display.py#L36), [L780-783](ui/ui_display.py#L780-L783)
- 카드 렌더 함수 `_draw_burner_card`: [L802-879](ui/ui_display.py#L802-L879)
- 툴바(설정/Mask/카메라/Dev/일시정지) 렌더: [L713-728](ui/ui_display.py#L713-L728)
- 상태별 카드 배경색은 `bsm.color`가 반환 (상태머신 `STATE_COLORS`).

---

## 4. 작업 후 테스트 체크리스트

- [ ] A: dev(`D`) ON에서 영상/카드 화구 번호가 크게 보이고, OFF에서는 원래 크기
- [ ] B: `WAIT_SECOND`(재벌대기) 카드에 "재벌대기"가 한 번만 표시
- [ ] C: 운영 모드 카드에 RMS 텍스트 없음 / dev 모드에서만 RMS 표시
- [ ] D: DONE_FIRST 카드 버튼 라벨 "바로 재벌", 클릭 시 즉시 재벌 카운트다운(진초록) 시작
- [ ] E: 카운트다운 30초 전 warn_30s 음 **1회**, 완료 순간 complete 음 **1회** (초벌·재벌 각각) / 계속 울리지 않음 / 비카운트 상태 오발 없음 / 음소거 토글(있다면) 동작
- [ ] F: 카운트다운 중 화구가 남은시간 적은 순으로 좌상단, 완료 후 ~8초 상단 유지 후 번호순 복귀, 클릭 타겟 정상
- [ ] G: 분할선 드래그로 비율 조절 / `V`로 영상 숨김(카드 전체) 토글 / 패널 넓히면 카드·번호가 실제로 커짐 / MIN·MAX 클램프 / 리사이즈 후 유지(저장 시)
- [ ] H: 카메라 연결됨→프레임 없음 전환 시 cam_lost 음 1회 / 끊김 지속 중 반복 안 함 / 음소거 토글 적용

---

## 5. 참고 — 손대지 않아도 되는 것 / 이미 된 것

- `manual_start()` 백엔드 동작(즉시 재벌) — **완료**. UI는 라벨만.
- `seconds_since_done` / `is_counting` / `remaining_seconds` property — **추가 완료**. 그대로 호출.
- 알림음 wav 3종(`warn_30s.wav`/`complete.wav`/`cam_lost.wav`) — **생성 완료**. 로드/재생만 구현.
- 영상 오버레이 RMS의 dev 가드 — **이미 적용됨**(작업 C는 '카드' RMS만 추가 가드).
